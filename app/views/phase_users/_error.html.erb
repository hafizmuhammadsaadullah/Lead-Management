<% if phase_user.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= I18n.t("errors.messages.not_saved",
                count: phase_user.errors.count,
                resource: phase_user.class.model_name.human.downcase)
      %>
    </h2>
    <ul>
      <% phase_user.errors.full_messages.each do |message| %>
        <li><span style="color : red;"><%= message %><span></li>
      <% end %>
    </ul>
  </div>
<% end %>